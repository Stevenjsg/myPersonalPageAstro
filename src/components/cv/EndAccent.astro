---
const { text = "fin del cv" } = Astro.props;
---

<div
  class="flex items-center justify-center gap-3 mt-16 font-mono text-[11px] tracking-[0.2em] uppercase text-[var(--text-mute)] dark:text-sunglow-700"
>
  <span class="block w-12 h-px bg-[var(--border)]"></span>
  {text}
  <span class="block w-12 h-px bg-[var(--border)]"></span>
</div>
